import { StyleSheet } from "react-native";

export const styles = StyleSheet.styles({
  container: {
    flex: 1,
    alignItems: "center",
    justifyContent: "center",
    backgroundColor: "gray",
  },
});
